# Server Configuration
PORT=8088
GIN_MODE=debug

# Database Configuration (from MCP)
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=<passworddb>
DB_NAME=strapi_ipl
DB_SSLMODE=disable

# Logging
LOG_LEVEL=debug
LOG_FORMAT=json

# DOKU Payment Configuration
DOKU_CLIENT_ID=BRN-0241-1762176502792
DOKU_SECRET_KEY=SK-PaILsZudZTytTSTNCmUV
DOKU_BASE_URL=https://api-sandbox.doku.com

# JWT Configuration (from Strapi)
JWT_SECRET=ByBlmB4Rzpb3irtNXwpm0A==

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

# Mayar Payment Configuration
MAYAR_AUTH_KEY=<authkey_from_mayar>
MAYAR_BASE_URL=https://api.mayar.id/hl/v1

# Scheduler Configuration
# Cron format: "seconds minutes hours day-of-month month day-of-week"
# Default: "0 0 0 1 * *" = At 00:00:00 on the 1st of every month
# For testing every 10 seconds: "*/10 * * * * *"
BILLING_CRON_EXPRESSION=0 0 0 1 * *